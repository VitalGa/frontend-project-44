import engineForAllGames from '../index.js';
import getRandomInt from '../getRandomInt.js';

const gameRulesGcd = 'What is the result of the expression?';


const gcdGame = () => engineForAllGames(gameRulesGcd, ????);

export default gcdGame;